<h2>New book</h2>
<%= form_with model: @book do |f| %>
  Title
  <%= f.text_field :title %>

  Opinion
  <%= f.text_area :body %>
  <%= f.submit "Create Book" %>
<% end %>




<% @books.each do |book| %>
  <%= book.title %>
  <%= book.body %>
  <%= book.user_id %>
<% end %>